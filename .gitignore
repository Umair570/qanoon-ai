# --- 1. Security (CRITICAL) ---
# Never upload these. They contain your API keys and system paths.
venv/
env/
.env

# --- 2. Python Internals ---
# These are junk files generated by Python.
__pycache__/
*.py[cod]
*.so

# --- 3. RAG Memory (ALLOWED NOW) ---
# We removed the lines blocking faiss_index so Render can see your AI brain.
# If you have a folder named 'chroma_db' or 'vectorstore' that you WANT to upload, remove them from here too.
chroma_db/
vectorstore/

# --- 4. Temp & Log files ---
*.tmp
*.log
*.log.*

# --- 5. System Files ---
.DS_Store
Thumbs.db

# --- 6. IDE Settings (Optional but recommended) ---
.vscode/
.idea/